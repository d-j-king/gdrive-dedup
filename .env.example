# Google Drive Dedup Configuration
# Copy this file to .env to customize settings

# Data directory (default: ~/.gdrive-dedup)
# GDRIVE_DEDUP_DATA_DIR=/path/to/data

# API rate limit in requests per second (default: 10)
# GDRIVE_DEDUP_RATE_LIMIT=10.0

# Page size for API requests (default: 1000)
# GDRIVE_DEDUP_PAGE_SIZE=1000

# Batch size for operations (default: 100)
# GDRIVE_DEDUP_BATCH_SIZE=100

# Minimum file size in bytes (default: 0)
# GDRIVE_DEDUP_MIN_FILE_SIZE=0

# Enable byte-by-byte comparison (default: false)
# GDRIVE_DEDUP_BYTE_COMPARE=false

# Log level (default: INFO)
# GDRIVE_DEDUP_LOG_LEVEL=INFO

# Log file path (default: None)
# GDRIVE_DEDUP_LOG_FILE=/path/to/logfile.log
